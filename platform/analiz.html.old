<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Zihin Platformu – Genel Analiz</title>

  <!-- Ortak Stil -->
  <link rel="stylesheet" href="style.css">

  <!-- Chart.js -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <!-- Heatmap (ileride kullanılacak) -->
  <script src="https://cdn.jsdelivr.net/npm/chartjs-chart-matrix@1.3.0/dist/chartjs-chart-matrix.min.js"></script>

  <style>
    .analiz-wrapper {
      width: 95%;
      max-width: 900px;
      margin: 20px auto;
      background: #fff;
      padding: 20px;
      border-radius: 14px;
      box-shadow: 0 4px 14px rgba(0,0,0,0.15);
    }

    h2 {
      text-align: center;
      margin-bottom: 25px;
      color: #1e3d59;
      font-size: 28px;
      font-weight: bold;
    }

    .filter-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      margin-bottom: 20px;
      gap: 10px;
    }

    select {
      padding: 8px 12px;
      border-radius: 8px;
      border: 1px solid #bbb;
      font-size: 16px;
    }

    .list-container {
      margin-top: 25px;
    }

    .sonuc-kart {
      background: #f7f9fc;
      border-radius: 10px;
      padding: 12px 16px;
      margin-bottom: 12px;
      border-left: 6px solid #4a90e2;
      transition: .25s;
      cursor: pointer;
    }

    .sonuc-kart:hover {
      transform: scale(1.02);
      background: #eef5ff;
    }

    .chart-section {
      margin-top: 35px;
      background: #fdfdfd;
      padding: 20px;
      border-radius: 14px;
      box-shadow: 0 3px 12px rgba(0,0,0,0.1);
    }

    canvas {
      width: 100% !important;
      max-height: 300px;
    }

    .nav-bar {
      display: flex;
      gap: 8px;
      padding: 10px;
      background: #f3f4f6;
      border-bottom: 1px solid #ddd;
    }

    .nav-bar button {
      padding: 8px 12px;
      border: none;
      border-radius: 8px;
      background: #4a90e2;
      color: #fff;
      cursor: pointer;
      font-size: 14px;
    }

    .nav-bar button:hover {
      background: #3578c6;
    }
  </style>
</head>

<body>

  <!-- ÜST MENÜ (Rol Dinamik) -->
  <div class="nav-bar" id="navBar"></div>

  <div class="analiz-wrapper">

    <h2>📊 Genel Performans Analizi</h2>

    <!-- FİLTRELER -->
    <div class="filter-row">

      <!-- Oyun Filtre -->
      <select id="oyunFiltre">
        <option value="hepsi">Tüm Oyunlar</option>
      </select>

      <!-- Tarih Filtre -->
      <select id="tarihFiltre">
        <option value="son">En Son</option>
        <option value="hafta">Son 1 Hafta</option>
        <option value="ay">Son 1 Ay</option>
        <option value="tum">Tüm Kayıtlar</option>
      </select>
    </div>

    <!-- SONUÇ LİSTESİ -->
    <div class="list-container" id="sonucListe"></div>

    <!-- TREND GRAFİK -->
    <div class="chart-section">
      <h3 style="text-align:center;margin-bottom:15px;">📈 Trend Analizi</h3>
      <canvas id="trendChart"></canvas>
    </div>

    <!-- BİLEŞEN KARŞILAŞTIRMA -->
    <div class="chart-section">
      <h3 style="text-align:center;margin-bottom:15px;">🧠 Bilişsel Karşılaştırma</h3>
      <canvas id="compareChart"></canvas>
    </div>

  </div>

  <!-- ====================================================== -->
  <!-- 🔵 NAVBAR – Rol Tabanlı Menü Kontrolü -->
  <!-- ====================================================== -->
  <script type="module">
    import { ROLES } from "./router.js";

    const role = localStorage.getItem("role");
    const nav = document.getElementById("navBar");

    if (role === ROLES.OGRETMEN) {
      nav.innerHTML = `
        <button onclick="window.location.href='teacher_panel.html'">
          👩‍🏫 Öğrenci Seçimine Dön
        </button>
      `;
    } else {
      nav.innerHTML = `
        <button onclick="window.location.href='index.html'">🏠 Ana Menü</button>
        <button onclick="window.location.href='sonuc.html'">📄 Sonuç</button>
      `;
    }
  </script>

  <!-- ANALİZ MOTORU -->
  <script type="module" src="analiz.js"></script>

</body>
</html>